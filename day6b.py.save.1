with open('day6data.txt') as f:
    lines = [line.rstrip() for line in f]

lettercount =[]
sum_total=0
first=1

for line in lines:
  if line == '':
    #print len(lettercount)
    sum_total+=len(lettercount)
    lettercount=[]
    first=1
    #print 'space'
  else:
    print len(lettercount)
    sum_total+=len(lettercount)
    lettercount=[]
    print 'space'    for i in line:
      if first == 1:
        lettercount.append(i)
        first=0
      else:
        if i not in lettercount:
          lettercount.remove(i)
          #print i


print sum_total
